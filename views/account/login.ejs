<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %> 

<%- messages() %> 

<% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<% } %>

<form method="post" action="/account/login">
  <div class="form-data">
    <label for="accountEmail">Email:</label>
    <input
      id="accountEmail"
      class="input-data"
      type="email"
      name="account_email"
      required
      value="<%= locals.account_email %>"
    />
  </div>
  <div class="form-data">
    <label for="accountPassword">Password:</label>
    <input
      class="input-data"
      id="accountPassword"
      type="password"
      name="account_password"
      required
      pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$"
      title="Passwords must be at least 12 characters and contain at least 1 number, 1
      capital letter and 1 special character"
    />
  </div>

  <input class="link" type="submit" value="Log In" />

  <p>No account? <a href="register">Sign-up</a></p>
</form>
