<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %> <%- messages() %> <% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<% } %>

<form action="/inv/add-inventory" method="post">
  <div class="form-data">
    <label for="invMake">Vehicle Make:</label>
    <input
      id="invMake"
      class="input-data"
      type="text"
      name="inv_make"
      id="invMake"
      required
      value="<%= locals.inv_make %>"
    />
  </div>

  <div class="form-data">
    <label for="invModel">Vehicle Model:</label>
    <input
      id="invModel"
      class="input-data"
      type="text"
      name="inv_model"
      id="invModel"
      required
      value="<%= locals.inv_model %>"
    />
  </div>

  <div class="form-data">
    <label for="invYear">Vehicle Year:</label>
    <input
      id="invYear"
      class="input-data"
      type="text"
      name="inv_year"
      min="1900"
      max="<%- thisYear %>"
      id="invYear"
      required
      title="Vehicle year cannot contain spaces or special characters."
      value="<%= locals.inv_year %>"
    />
  </div>

  <div class="form-data">
    <label for="invDescription">Vehicle Description:</label>
    <textarea
      id="invDescription"
      class="input-data"
      name="inv_description"
      id="invDescription"
      required
    >
<%= locals.inv_description %></textarea
    >
  </div>

  <div class="form-data">
    <label for="invImage">Vehicle Image:</label>
    <input
      id="invImage"
      class="input-data"
      type="text"
      name="inv_image"
      id="invImage"
      required
      value="/images/vehicles/no-image.png"
    />
  </div>

  <div class="form-data">
    <label for="invThumbnail">Vehicle Thumbnail:</label>
    <input
      id="invThumbnail"
      class="input-data"
      type="text"
      name="inv_thumbnail"
      id="invThumbnail"
      required
      value="/images/vehicles/no-image-tn.png"
    />
  </div>

  <div class="form-data">
    <label for="invPrice">Vehicle Price:</label>
    <input
      id="invPrice"
      class="input-data"
      type="number"
      step="0.01"
      min="0"
      name="inv_price"
      id="invPrice"
      required
      value="<%= locals.inv_price %>"
    />
  </div>

  <div class="form-data">
    <label for="invMiles">Vehicle Miles:</label>
    <input
      id="invMiles"
      class="input-data"
      min="0"
      type="number"
      name="inv_miles"
      id="invMiles"
      required
      value="<%= locals.inv_miles %>"
    />
  </div>

  <div class="form-data">
    <label for="invColor">Vehicle Color:</label>
    <input
      id="invColor"
      class="input-data"
      type="text"
      name="inv_color"
      id="invColor"
      required
      value="<%= locals.inv_color %>"
    />
  </div>

  <div class="form-data">
    <label for="classificationId">Vehicle Classification:</label>
    <select name="classification_id" id="classificationId">
      <%- classifications %>
    </select>
  </div>

  <input class="link" type="submit" value="Add Vehicle" />
</form>
